const functionConcatenate = (string1, string2, number) => {

//this simple function takes two strings and a number as parameters.

result = number*2;

    return "the concatenated string is " + string1 + string2 + result.toString();

    //concatenates the two strings, plus the number (converted to string with the toString() method)
}

console.log(functionConcatenate("a", "b", 5)); //prints "the concatenated string is ab10"




